# Contributing to Boomstick

Use a focused feature branch and keep changes small. Run `npm test`, `npm run lint`, `npm run typecheck`, `npm run format:check`, and `npm run build` before opening a pull request.

Follow `AGENTS.md`, include tests for behavior changes, and update documentation and the changelog when users will notice the change.
