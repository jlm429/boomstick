# Deployment skill

The public site is deployed to GitHub Pages under `/boomstick/`. Keep Vite `base` aligned with that repository path. Build before deployment and preserve the Pages workflow's least-privilege permissions. See `docs/deployment.md`.
