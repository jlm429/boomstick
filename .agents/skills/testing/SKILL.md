# Testing skill

Use Vitest plus React Testing Library for behavior, transitions, stores, and pure utilities. Mock Canvas scene modules in UI tests when necessary. Do not use WebGL visual snapshots; validate browser behavior separately for rendering and pointer lock.
